# Normalise line endings so that a checkout on Windows cannot introduce CRLF
# into shell scripts, Dockerfiles or Python sources that are executed on Linux
# and inside the containers.
* text=auto eol=lf

# Binary assets must never be converted.
*.woff2 binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
